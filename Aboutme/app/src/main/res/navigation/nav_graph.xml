<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/FirstFragment">

    <!-- FirstFragment -->
    <fragment
        android:id="@+id/FirstFragment"
        android:name="com.example.aboutme.FirstFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_first">
        <action
            android:id="@+id/action_FirstFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
        <!-- Add this action to navigate from FirstFragment to ThirdFragment -->
        <action
            android:id="@+id/action_FirstFragment_to_ThirdFragment"
            app:destination="@id/thirdFragment" />
    </fragment>

    <!-- SecondFragment -->
    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.example.aboutme.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">
        <action
            android:id="@+id/action_SecondFragment_to_FirstFragment"
            app:destination="@id/FirstFragment" />
    </fragment>

    <!-- ThirdFragment -->
    <fragment
        android:id="@+id/thirdFragment"
        android:name="com.example.aboutme.ThirdFragment"
        android:label="contact"
        tools:layout="@layout/fragment_third" />

</navigation>